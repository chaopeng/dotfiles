# Chao's dotfiles

This repo uses [dotman][] to manage the symlinks for dotfiles. It is very easy to
use, just 1 line of command:

```sh
make links 
```

Each subdir includes config for one software so it is easy to maintance.

This also includes some softwares installation:

```sh
make arch-install
```

<!-- xrefs -->

[dotman]: hhttps://github.com/elmhuangyu/dotman
